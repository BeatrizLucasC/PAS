SELECT id_pergunta, resposta
FROM respostas
WHERE id_utilizador = '{{appsmith.store.autenticacao.nif}}'
  AND dominio = 'ambiental'
  AND ano = (
    SELECT MAX(ano)
    FROM respostas
    WHERE id_utilizador = '{{appsmith.store.autenticacao.nif}}'
      AND dominio = 'ambiental'
  );